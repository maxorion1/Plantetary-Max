import { Hono } from 'hono';

export class SubstrateDO {
  state: DurableObjectState;
  constructor(state: DurableObjectState) {
    this.state = state;
  }

  async fetch(request: Request): Promise<Response> {
    return new Response("SubstrateDO Active");
  }
}

export class BrokerDO {
  state: DurableObjectState;
  constructor(state: DurableObjectState) {
    this.state = state;
  }

  async fetch(request: Request): Promise<Response> {
    return new Response("BrokerDO Active");
  }
}

const app = new Hono();

app.get("/", (c) => c.text("Portal‑OS Worker Active (Lane 1)"));
app.get("/health", (c) => c.json({ status: "ok", lane: 1 }));

export default app;
